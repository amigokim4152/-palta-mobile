# Static mobile structure prototype

Open `index.html` in a browser.

Purpose:
- review information hierarchy before Expo bootstrap
- verify that Home does not look like a portal/menu
- verify Neighborhood map/list composition
- verify five-tab shell balance

This is NOT final visual design.
Neutral colors are intentional because final brand color/typography values are not frozen yet.
No external library, API or network request is required.
