# Physical iPhone must reach the Mac on the same LAN for the local API.
# Replace this example API host with the Mac's actual LAN IP when needed.
EXPO_PUBLIC_PALTA_API_BASE_URL=http://192.168.1.20:8787
EXPO_PUBLIC_ENV=development
# Chile-wide production Map Core. Override only for an intentional preview.
EXPO_PUBLIC_MAP_STYLE_URL=https://palta-map-edge.kimeuisin.workers.dev/maps/cl/style.json
